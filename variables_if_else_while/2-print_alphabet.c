#include <stdio.h>
#include <ctype.h>
/**
 * main - Printing the alphabet, coming back to change it
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++) {
}
putchar('\n');
return (0);
}
