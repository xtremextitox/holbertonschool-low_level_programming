#include <stdio.h>
#include <stdlib.h>
/**
 * main - Printing numbers from 0 to 10
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	for (c = 1; c <= 9; c++)
		putchar(c);
		printf('\n');
	
	return (0);
}
