#include <stdio.h>
#include <stdlib.h>
/**
 * main - Printing numbers from 0 to 10
 * Return: Always 0 (Success)
 */
int main(void)
{
for (int c = 0; c <= 9; c++)
{
printf("%d", c);
}
printf("\n");
return (0);
}
