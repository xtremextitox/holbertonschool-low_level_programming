#include <stdio.h>
/**
 * main - Positive or negative task to learn how to use if and else
 * Return: Always 0 (Success)
 */
int main(void)
{
if (n > 0) {
	printf(is positive);
}
else if (n = 0) {
	printf(is zero);
}
else if (n < 0) {
	printf(is negative);
}
return (0);
}
